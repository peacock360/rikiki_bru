services:
  web:
    build:
      context: .
      args:
        TELEGRAM_API_ID: ${TELEGRAM_API_ID}
        TELEGRAM_API_HASH: ${TELEGRAM_API_HASH}
        BASE_URL: ${BASE_URL:-https://web.telegram.org/a/}
    ports:
      - "8080:80"
    restart: unless-stopped
